patch:
  schema_list:
    - schema: rime_ice              # 雾凇拼音（全拼）
    - schema: double_pinyin_flypy   # 小鹤双拼

  # 开关统一由各 schema 的 custom 文件决定；此处不定义，避免与 rime_ice/double_pinyin_flypy 的 switches: [] 冲突